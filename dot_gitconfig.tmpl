[core]
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true

[merge]
    conflictstyle = zdiff3

[user]
	email = {{ .github.email }}
	name = {{ .github.name }}
